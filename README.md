📝 Registration Form with Validation
This is a simple and interactive Registration Form built using HTML and JavaScript. It includes real-time client-side validation for user inputs such as email, phone number, and password to ensure correct data before submission.

*Features
.Username input field
.Email validation using Regular Expressions
.Phone number validation (10 digits only)
.Password length validation (minimum 8 characters)
.Confirm password matching
.Instant alert messages for invalid input
.Successful registration confirmation
.Auto form reset after successful submission

*Technologies Used
HTML5 – For form structure
JavaScript (Vanilla JS) – For validation logic

*Validation Rules
|Email must follow a valid format (example: abc@gmail.com)
|Phone number must contain exactly 10 digits
|Password must contain at least 8 characters
|Password and Confirm Password must match
